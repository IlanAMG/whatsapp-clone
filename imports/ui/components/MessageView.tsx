import React from 'react'

import StyledMessageView from '../elements/StyledMessageView'

export const MessageView = (props:any):JSX.Element => {
    return (
        <StyledMessageView>
            
        </StyledMessageView>
    )
}
